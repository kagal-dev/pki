# Changelog

All notable changes to `@kagal/build-tsdocs` will be
documented in this file.

## [Unreleased]

### Added

- `newDocumentsHook()` — unbuild `build:done` hook for
  TSDoc extraction via `tsdoc-markdown`
- `DocumentsManifest`, `ExportManifest`,
  `DocumentsHookOptions` types
- `DEFAULT_OUTPUT_DIRECTORY` constant
- `DocEntry` re-exported from `tsdoc-markdown`
- `VERSION` constant
